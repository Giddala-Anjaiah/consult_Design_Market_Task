{
  "name": "portfolio-management-system",
  "version": "1.0.0",
  "description": "Full-stack Portfolio Management System",
  "main": "server/server.js",
  "scripts": {
    "start": "node server/server.js",
    "dev": "nodemon server/server.js",
    "build": "cd client && npm install && npm run build",
    "build-and-start": "cd client && npm install && npm run build && cd .. && node server/server.js",
    "postinstall": "cd client && npm install && npm run build",
    "heroku-postbuild": "cd client && npm install && npm run build"
  },
  "dependencies": {
    "bcryptjs": "^2.4.3",
    "cloudinary": "^2.9.0",
    "cors": "^2.8.5",
    "dotenv": "^16.0.3",
    "express": "^4.18.2",
    "express-validator": "^7.0.1",
    "jsonwebtoken": "^9.0.0",
    "mongoose": "^7.0.3",
    "multer": "^1.4.5-lts.1",
    "multer-storage-cloudinary": "^2.2.1"
  },
  "engines": {
    "node": ">=16.0.0",
    "npm": ">=8.0.0"
  },
  "keywords": [
    "portfolio",
    "react",
    "nodejs",
    "express",
    "mongodb"
  ],
  "author": "Portfolio Developer",
  "license": "MIT"
}
